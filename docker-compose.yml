services:
  router-01:
    image: ubuntu:22.04
    container_name: router-01
    ports:
      - "2221:22"
    volumes:
      - "${HOME}/.ssh/id_rsa.pub:/tmp/authorized_keys:ro"
    command: >
      bash -c "
        apt-get update -qq &&
        apt-get install -y -qq openssh-server sudo python3 chrony &&
        useradd -m -s /bin/bash ubuntu &&
        usermod -aG sudo ubuntu &&
        echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers &&
        mkdir -p /home/ubuntu/.ssh &&
        cp /tmp/authorized_keys /home/ubuntu/.ssh/authorized_keys &&
        chown -R ubuntu:ubuntu /home/ubuntu/.ssh &&
        chmod 700 /home/ubuntu/.ssh &&
        chmod 600 /home/ubuntu/.ssh/authorized_keys &&
        echo 'PermitRootLogin no' >> /etc/ssh/sshd_config &&
        echo 'Protocol 2' >> /etc/ssh/sshd_config &&
        echo 'LoginGraceTime 60' >> /etc/ssh/sshd_config &&
        mkdir -p /run/sshd &&
        /usr/sbin/sshd -D
      "

  router-02:
    image: ubuntu:22.04
    container_name: router-02
    ports:
      - "2222:22"
    volumes:
      - "${HOME}/.ssh/id_rsa.pub:/tmp/authorized_keys:ro"
    command: >
      bash -c "
        apt-get update -qq &&
        apt-get install -y -qq openssh-server sudo python3 &&
        useradd -m -s /bin/bash ubuntu &&
        usermod -aG sudo ubuntu &&
        echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers &&
        mkdir -p /home/ubuntu/.ssh &&
        cp /tmp/authorized_keys /home/ubuntu/.ssh/authorized_keys &&
        chown -R ubuntu:ubuntu /home/ubuntu/.ssh &&
        chmod 700 /home/ubuntu/.ssh &&
        chmod 600 /home/ubuntu/.ssh/authorized_keys &&
        echo 'PermitRootLogin no' >> /etc/ssh/sshd_config &&
        mkdir -p /run/sshd &&
        /usr/sbin/sshd -D
      "

  switch-01:
    image: ubuntu:22.04
    container_name: switch-01
    ports:
      - "2223:22"
    volumes:
      - "${HOME}/.ssh/id_rsa.pub:/tmp/authorized_keys:ro"
    command: >
      bash -c "
        apt-get update -qq &&
        apt-get install -y -qq openssh-server sudo python3 chrony vsftpd &&
        useradd -m -s /bin/bash ubuntu &&
        usermod -aG sudo ubuntu &&
        echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers &&
        mkdir -p /home/ubuntu/.ssh &&
        cp /tmp/authorized_keys /home/ubuntu/.ssh/authorized_keys &&
        chown -R ubuntu:ubuntu /home/ubuntu/.ssh &&
        chmod 700 /home/ubuntu/.ssh &&
        chmod 600 /home/ubuntu/.ssh/authorized_keys &&
        echo 'PermitRootLogin no' >> /etc/ssh/sshd_config &&
        mkdir -p /run/sshd &&
        /usr/sbin/sshd -D
      "

  switch-02:
    image: ubuntu:22.04
    container_name: switch-02
    ports:
      - "2224:22"
    volumes:
      - "${HOME}/.ssh/id_rsa.pub:/tmp/authorized_keys:ro"
    command: >
      bash -c "
        apt-get update -qq &&
        apt-get install -y -qq openssh-server sudo python3 chrony &&
        useradd -m -s /bin/bash ubuntu &&
        usermod -aG sudo ubuntu &&
        echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers &&
        mkdir -p /home/ubuntu/.ssh &&
        cp /tmp/authorized_keys /home/ubuntu/.ssh/authorized_keys &&
        chown -R ubuntu:ubuntu /home/ubuntu/.ssh &&
        chmod 700 /home/ubuntu/.ssh &&
        chmod 600 /home/ubuntu/.ssh/authorized_keys &&
        echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config &&
        mkdir -p /run/sshd &&
        /usr/sbin/sshd -D
      "

  firewall-01:
    image: ubuntu:22.04
    container_name: firewall-01
    ports:
      - "2225:22"
    volumes:
      - "${HOME}/.ssh/id_rsa.pub:/tmp/authorized_keys:ro"
    command: >
      bash -c "
        apt-get update -qq &&
        apt-get install -y -qq openssh-server sudo python3 vsftpd &&
        useradd -m -s /bin/bash ubuntu &&
        usermod -aG sudo ubuntu &&
        echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers &&
        mkdir -p /home/ubuntu/.ssh &&
        cp /tmp/authorized_keys /home/ubuntu/.ssh/authorized_keys &&
        chown -R ubuntu:ubuntu /home/ubuntu/.ssh &&
        chmod 700 /home/ubuntu/.ssh &&
        chmod 600 /home/ubuntu/.ssh/authorized_keys &&
        echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config &&
        mkdir -p /run/sshd &&
        /usr/sbin/sshd -D
      "